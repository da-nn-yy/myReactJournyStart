// import { useNavigate } from "react-router-dom";  //Help us for Route using useNavigate function.
import React from 'react'

function Profile() {
  // let navigate = useNavigate()   /// function for navigate
  return (
    <div>
      <h1>Profile</h1>
  {/* <button onClick={() => navigate('/home')}>Go to Home page</button> */} {/*This is useNavigate use */}
    </div>
  )
}

export default Profile
